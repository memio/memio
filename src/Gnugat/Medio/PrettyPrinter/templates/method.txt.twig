    public function {{ method.name }}(
        {%- if should_inline_arguments(method) -%}
            {% include 'inline_argument_collection.txt.twig' with { 'argumentCollection': method.argumentCollection } only %}
        {%- else -%}
            {% include 'multiline_argument_collection.txt.twig' with { 'argumentCollection': method.argumentCollection } only %}
        {%- endif -%}
    )
    {
    }
